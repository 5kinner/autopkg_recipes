Description: |
  Downloads the Staged version of a file from a local file share.
Identifier: com.github.5kinner-recipes.download.FileZilla

Input:
  NAME: FileZilla

Process:

- Processor: FileFinder
  Arguments:
    pattern: '%RECIPE_CACHE_DIR%/manual_download/*.bz2'